<UserControl x:Class="ProjectTFDB.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="API Settings" FontSize="18" FontWeight="SemiBold" />

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,10,12,0" Text="SteamID64" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,10,0,0" Text="{Binding SteamId64, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,10,12,0" Text="Steam Web API Key" VerticalAlignment="Center" />
            <TextBox Grid.Row="2" Grid.Column="1" Margin="0,10,0,0" Text="{Binding SteamApiKey, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,10,12,0" Text="Backpack.tf API Key" VerticalAlignment="Center" />
            <TextBox Grid.Row="3" Grid.Column="1" Margin="0,10,0,0" Text="{Binding BackpackApiKey, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,14,0,0">
                <Button Content="Save" Width="120" Margin="0,0,8,0" Command="{Binding SaveSettingsCommand}" />
                <Button Content="Refresh" Width="120" Margin="0,0,8,0" Command="{Binding RefreshCommand}" />
                <Button Content="Import Schema" Width="140" Margin="0,0,8,0" Command="{Binding ImportSchemaCommand}" />
                <Button Content="Open Cache Folder" Width="160" Command="{Binding OpenCacheFolderCommand}" />
            </StackPanel>

            <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Margin="0,14,0,0">
                <TextBlock Text="Schema" FontSize="16" FontWeight="SemiBold" />
                <TextBlock>
                    <Run Text="{Binding SchemaStatus, Mode=OneWay}" />
                    <Run Text=" • " />
                    <Run Text="{Binding SchemaCount, Mode=OneWay}" />
                    <Run Text=" items" />
                </TextBlock>
                <TextBlock Text="{Binding SchemaPath}" TextWrapping="Wrap" Opacity="0.8" />

                <TextBlock Text="Errors" Margin="0,12,0,0" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{Binding LastError}" Foreground="Tomato" TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
