<UserControl x:Class="ProjectTFDB.Views.DealsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8">
        <DataGrid ItemsSource="{Binding Deals}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60" />
                <DataGridTextColumn Header="Buy (keys)" Binding="{Binding BuyKeys}" Width="110" />
                <DataGridTextColumn Header="Sell (keys)" Binding="{Binding SellKeys}" Width="110" />
                <DataGridTextColumn Header="Spread (keys)" Binding="{Binding SpreadKeys}" Width="120" />
                <DataGridTextColumn Header="Defindex" Binding="{Binding Defindex}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

